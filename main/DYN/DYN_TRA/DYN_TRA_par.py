import numpy as np

# Parameters for Module DYN_TRA

DYN_TRA_par = {
    "SCpos_SSB_ini" : np.array([0,0,0]), # [km]   Initial relative position of spacecraft on SSB reference frame
    "SCvel_SSB_ini" : np.array([0,0,0])  # [km/s] Initial relative velocity of spacecraft on SSB reference frame
}